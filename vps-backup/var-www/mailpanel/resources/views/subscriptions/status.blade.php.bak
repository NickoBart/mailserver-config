@extends('layouts.app')

@section('content')
<div class="max-w-2xl mx-auto p-6 bg-white rounded-lg shadow">
    <h1 class="text-2xl font-bold mb-4">Mi Suscripción</h1>

    @if($subscription)
        <p><strong>Estado:</strong> {{ ucfirst($subscription->status) }}</p>
        <p><strong>Vence el:</strong> {{ $subscription->expires_at->format('d-m-Y') }}</p>
        <p><strong>Plan:</strong> {{ $subscription->plan_name ?? '—' }}</p>
        <a href="#" class="inline-block mt-4 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Renovar</a>
    @else
        <p>No tienes una suscripción activa.</p>
        <a href="{{ route('pricing') }}" class="inline-block mt-4 px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700">Ver planes</a>
    @endif
</div>
@endsection
