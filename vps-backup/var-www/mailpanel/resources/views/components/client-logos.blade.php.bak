@props(['logos'])
<div class="py-16 bg-gray-50 dark:bg-gray-900">
  <h2 class="text-2xl font-bold text-center mb-8 dark:text-gray-100">Clientes que confían en nosotros</h2>
  <div class="max-w-5xl mx-auto grid grid-cols-2 sm:grid-cols-4 gap-6 items-center">
    @foreach($logos as $logo)
      <div class="p-4 bg-white dark:bg-gray-800 rounded-lg shadow flex items-center justify-center">
        <img src="{{ asset(\$logo) }}" alt="Logo cliente" class="max-h-12">
      </div>
    @endforeach
  </div>
</div>
