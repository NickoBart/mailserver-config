@props(['testimonios'])
<div class="my-16">
  <h2 class="text-2xl font-bold text-center mb-8 dark:text-gray-100">Lo que dicen nuestros clientes</h2>
  <div id="testimonials" class="splide mx-auto max-w-4xl">
    <div class="splide__track">
      <ul class="splide__list">
        @foreach($testimonios as $t)
          <li class="splide__slide p-6 bg-white dark:bg-gray-800 rounded-lg shadow">
            <div class="flex items-center mb-4">
              <img src="{{ asset(\$t['foto']) }}" alt="{{ \$t['name'] }}" class="h-12 w-12 rounded-full mr-4">
              <div>
                <p class="font-semibold">{{ \$t['name'] }}</p>
                <p class="text-sm text-gray-500 dark:text-gray-400">{{ \$t['cargo'] }}</p>
              </div>
            </div>
            <p class="text-gray-700 dark:text-gray-300">“{{ \$t['texto'] }}”</p>
          </li>
        @endforeach
      </ul>
    </div>
  </div>
</div>
