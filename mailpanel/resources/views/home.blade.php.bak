{{-- resources/views/home.blade.php --}}
<x-guest-layout>

  {{-- Hero con imagen de fondo y texto overlay --}}
  <section id="hero" class="relative w-full h-[500px] bg-center bg-cover" style="background-image:url('{{ asset('images/placeholder-hero.jpg') }}')">
    <div class="absolute inset-0 bg-black bg-opacity-50"></div>
    <div class="relative z-10 max-w-screen-xl mx-auto px-4 py-32 flex flex-col sm:flex-row items-center gap-8">
      <div class="sm:w-1/2 space-y-6 text-white">
        <h1 class="text-5xl font-extrabold leading-tight">
          Tu Correo Corporativo
          <span class="text-blue-400">Profesional y Seguro</span>
        </h1>
        <p class="text-xl max-w-lg">
          Correo corporativo cifrado y sin anuncios: privacidad total y profesionalismo en cada mensaje.
        </p>
        <a href="{{ route('pricing') }}" class="inline-block bg-blue-600 text-white font-semibold px-8 py-4 rounded-lg shadow hover:bg-blue-700 transition">
          Ver planes
        </a>
      </div>
      <div class="sm:w-1/2">
        <img src="{{ asset('images/placeholder-device.png') }}"
             alt="Vista previa de la aplicación"
             class="w-full rounded-lg shadow-lg mx-auto">
      </div>
    </div>
  </section>

  {{-- Sección de Beneficios con imagen lateral --}}
  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
      <div>
        <img src="{{ asset('images/placeholder-benefit.jpg') }}"
             alt="Beneficios de Connectia Mail"
             class="w-full rounded-lg shadow-lg">
      </div>
      <div class="space-y-8">
        @foreach($features as $feature)
          <div class="flex items-start gap-4">
            <div class="flex-shrink-0 w-12 h-12 flex items-center justify-center bg-blue-100 rounded-full">
              <i class="{{ $feature['icon'] }} text-blue-600 text-xl"></i>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-gray-800">{{ $feature['title'] }}</h3>
              <p class="text-gray-600">{{ $feature['text'] }}</p>
            </div>
          </div>
        @endforeach
      </div>
    </div>
  </section>

  {{-- Testimonios con foto grande --}}
  <section class="py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h2 class="text-3xl font-semibold text-gray-800 mb-8">Lo que dicen nuestros clientes</h2>
      <div class="relative">
        <img src="{{ asset('images/placeholder-testimonial.jpg') }}"
             alt="Testimonio destacado"
             class="w-full rounded-lg shadow-lg mb-8">
        <blockquote class="absolute bottom-8 left-1/2 transform -translate-x-1/2 bg-white bg-opacity-90 p-6 rounded-lg shadow">
          <p class="text-lg italic">“Desde que migramos a Connectia, nuestra comunicación es más segura y rápida.”</p>
          <footer class="mt-4 font-semibold text-gray-700">— Juan Pérez, Pyme Saludable</footer>
        </blockquote>
      </div>
    </div>
  </section>

  {{-- Logos de clientes en carrusel --}}
  <section class="py-12 bg-gray-50">
    <div class="max-w-5xl mx-auto px-4">
      <h2 class="text-2xl font-semibold text-gray-800 text-center mb-8">Clientes que confían en nosotros</h2>
      <div class="flex overflow-x-auto gap-6">
        @foreach($logos as $logo)
          <div class="flex-shrink-0 w-48 h-32 bg-white rounded-lg shadow flex items-center justify-center">
            <img src="{{ asset($logo) }}" alt="Logo cliente" class="max-h-16 object-contain">
          </div>
        @endforeach
      </div>
    </div>
  </section>

  {{-- CTAs secundarios con fondo de imagen --}}
  <section class="relative w-full h-[300px] bg-center bg-cover" style="background-image:url('{{ asset('images/placeholder-cta-bg.jpg') }}')">
    <div class="absolute inset-0 bg-black bg-opacity-50"></div>
    <div class="relative z-10 flex flex-col sm:flex-row items-center justify-center h-full gap-6 px-4">
      <a href="{{ url('/chat') }}" class="inline-block bg-blue-600 text-white font-semibold px-8 py-4 rounded-lg shadow hover:bg-blue-700 transition">
        Chatea con nosotros
      </a>
      <a href="{{ url('/signup?domain=true') }}" class="inline-block px-8 py-4 border border-white text-white font-semibold rounded-lg hover:bg-white hover:text-gray-900 transition">
        Ya tengo dominio
      </a>
    </div>
  </section>

</x-guest-layout>
